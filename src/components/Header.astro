---
import { SITE } from "../data/site";
const nav = [
  ["Work", "/work"],
  ["Services", "/services"],
  ["About", "/about"],
  ["Contact", "/contact"],
];
const path = Astro.url.pathname.replace(/\/$/, "") || "/";
---

<header class="site-header">
  <div class="wrap bar">
    <a class="brand" href="/" aria-label={SITE.name}>
      <img
        class="brand-logo"
        src="/images/417webco-logo-220.png"
        width="220"
        height="92"
        alt={SITE.name}
        decoding="async"
      />
    </a>
    <nav>
      {nav.map(([label, href]) => (
        <a href={href} class:list={{ active: path === href }}>{label}</a>
      ))}
    </nav>
    <div class="actions">
      <button id="theme-toggle" aria-label="Toggle dark mode">◐</button>
      <a class="btn btn-primary cta" href={`mailto:${SITE.email}`}>Email me</a>
    </div>
  </div>
</header>

<script>
  document.getElementById("theme-toggle")?.addEventListener("click", () => {
    const root = document.documentElement;
    const dark = root.dataset.theme ? root.dataset.theme === "dark" : true;
    root.dataset.theme = dark ? "light" : "dark";
    localStorage.setItem("theme", root.dataset.theme);
  });
</script>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 10;
    background: color-mix(in srgb, var(--bg-raised) 88%, transparent);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--line);
  }
  .bar {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    padding-top: 0.8rem;
    padding-bottom: 0.8rem;
  }
  .brand {
    display: inline-flex;
    align-items: center;
    text-decoration: none;
    margin-right: auto;
    line-height: 0;
  }
  .brand-logo {
    height: clamp(39px, 5.3vw, 51px);
    width: auto;
    display: block;
  }
  /* Cream letters need a dark backing on the white light-theme bar */
  :root[data-theme="light"] .brand-logo {
    background: var(--teal-900);
    padding: 0.28rem 0.5rem;
    border-radius: 10px;
  }
  nav { display: flex; gap: 1.1rem; flex-wrap: wrap; }
  nav a {
    color: var(--ink-soft);
    text-decoration: none;
    font-weight: 600;
    font-size: 0.95rem;
  }
  nav a:hover, nav a.active { color: var(--accent); }
  .actions { display: flex; align-items: center; gap: 0.6rem; }
  #theme-toggle {
    background: none;
    border: 1px solid var(--line);
    border-radius: 999px;
    color: var(--ink-soft);
    width: 2.1rem;
    height: 2.1rem;
    cursor: pointer;
    font-size: 1rem;
  }
  .cta { padding: 0.5rem 1.1rem; font-size: 0.9rem; }
  @media (max-width: 640px) {
    nav { display: none; }
  }
</style>
